<?xml version="1.0" encoding="UTF-8"?>
<!-- Tab导航 最新版 -->
<TabHost xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@android:id/tabhost"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent" >
    <RelativeLayout
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">
        <FrameLayout
            android:id="@android:id/tabcontent"
            android:layout_width="fill_parent"
            android:layout_height="fill_parent"
            android:background="#FFF" >
        </FrameLayout>
        <!-- TabWidget管理所有的选项卡，id名是android指定的 -->
        <TabWidget
            android:id="@android:id/tabs"
            android:layout_width="fill_parent"
            android:layout_height="fill_parent"
            android:visibility="gone" />
        <!-- Frame下放置单选群组替代TAB效果 -->
        <RadioGroup
            android:id="@+id/main_radio"
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:background="#FFFFFF"
            android:gravity="center_vertical"
            android:orientation="horizontal"
            android:layout_alignParentBottom="true" >
            <RadioButton
                android:id="@+id/tab_icon_weixin"
                style="@style/Tab"
                android:checked="true"
                android:drawableTop="@drawable/button_background"
                android:text="换乘" />
            <RadioButton
                android:id="@+id/tab_icon_address"
                style="@style/Tab"
                android:checked="false"
                android:drawableTop="@drawable/button_background2"
                android:text="地图" />
            <RadioButton
                android:id="@+id/tab_icon_find"
                style="@style/Tab"
                android:drawableTop="@drawable/button_background3"
                android:text="我" />
        </RadioGroup>
    </RelativeLayout>
</TabHost>